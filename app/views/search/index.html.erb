<div class="bg-white">
  <%# TODO: Show empty page state %>
  <div class="mx-auto max-w-2xl px-4 py-16 sm:px-6 lg:max-w-7xl lg:px-8 grid grid-cols-2">
    <div class="mt-6 grid grid-cols-1 gap-x-6 gap-y-10 sm:grid-cols-2 lg:grid-cols-2 xl:gap-x-8">
      <% @properties.each do |property| %>
        <%= render 'properties/property_item', property: property, minimal_details: false  %>
      <% end %>
    </div>
    <div id="map"></div>
  </div>
</div>

<div id="map"></div>
